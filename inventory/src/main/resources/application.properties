spring.application.name=inventory

# DEFAULT PROFILE (for local run)
# Switch profile manually
spring.profiles.active=dev
#spring.profiles.active=prod
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#unauth url
security.permit-all=/auth/login

#toke expire time (12 h/ 5m/ 10s/ 1d)
jwt.expiration= 12h

# Multiple allowed origins
app.cors.allowed-origins=http://localhost:5173,https://dev.starserviceinventory.cloud,https://app.starserviceinventory.cloud